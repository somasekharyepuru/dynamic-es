import { Options } from "highcharts";

export const Chart3dMergerOptions: Options = {
  chart: {
    options3d: {
      enabled: true,
      alpha: 45
    }
  }
}
export const Chart3dDeMergerOptions: Options = {
  chart: {
    options3d: {
      enabled: false,
    }
  }
}
